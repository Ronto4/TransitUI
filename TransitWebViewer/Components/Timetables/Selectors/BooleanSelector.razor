@implements ISelector<bool>

<div class="form-check-inline">
    <input class="form-check-input" type="checkbox" value="" checked="checked" id="checkbox-@GuidForId" @onchange="e => OnUpdate(e.Value is true)">
    <label class="form-check-label" for="checkbox-@GuidForId">
        @Text
    </label>
</div>

@code {
    [Parameter] public required string Text { get; init; }
    [Parameter] public required Action<bool> OnUpdate { get; init; }
    private Guid GuidForId { get; } = Guid.NewGuid();
}
